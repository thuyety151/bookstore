export interface IBookType {
  id: string;
  name: string;
}
export interface IBestSellerType {
  id: string;
  name: string;
  attribute: string;
  attriuteId:string;
  author: string;
  authorId:string;
  regularPrice: string;
  salePrice: string;
  imageUrl: string;
}
