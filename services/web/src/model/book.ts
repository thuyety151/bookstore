export interface BookType {
  id: string;
  name: string;
}
export interface Book {
  id: string;
  name: string;
  attribute: string;
  attriuteId: string;
  author: string;
  authorId: string;
  regularPrice: string;
  salePrice: string;
  imageUrl: string;
}
